<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ligue des Collègues - Paris 1N2</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-950 text-white min-h-screen">
    <nav class="bg-gray-800 p-4 shadow-lg border-b border-gray-700 flex justify-between items-center">
        <span class="text-xl font-black italic text-blue-400">🏆 LIGUE</span>
        <div class="flex items-center gap-4">
            <span class="text-sm">Salut, <strong><%= user.username %></strong></span>
            <a href="/logout" class="text-xs bg-red-900/50 hover:bg-red-800 px-3 py-1 rounded">Déconnexion</a>
        </div>
    </nav>

    <main class="max-w-4xl mx-auto p-6 space-y-10">
        <section class="bg-gray-900 p-8 rounded-3xl border border-gray-800 shadow-2xl">
            <h3 class="text-xl font-bold mb-8 flex items-center gap-3">⚽ Placer un pronostic 1N2</h3>
            
            <form action="/bet" method="POST" class="space-y-8">
                <div>
                    <label class="block text-xs uppercase text-gray-500 mb-2 font-bold">Choisir le match :</label>
<select name="matchId" id="matchSelect" class="w-full bg-gray-800 border border-gray-700 p-4 rounded-xl font-semibold text-white" required>
    <option value="">-- Sélectionnez un match --</option>
    <% matches.forEach(match => { 
        // On vérifie si l'ID du match est dans la liste des matchs déjà pariés
        const alreadyBetted = bettedMatchIds.includes(match._id.toString());
        
        // On n'affiche le match QUE si l'utilisateur n'a pas encore parié dessus
        if (!alreadyBetted) { 
    %>
        <option value="<%= match._id %>" data-c1="<%= match.code1 %>" data-c2="<%= match.code2 %>">
            <%= match.teams %>
        </option>
    <% 
        } // Fin du if
    }) // Fin du forEach %>
</select>
                </div>

                <div class="grid grid-cols-3 gap-6">
                    <label class="cursor-pointer group">
                        <input type="radio" name="prediction" value="1" class="hidden peer" required>
                        <div class="peer-checked:bg-blue-600 ... flex flex-col items-center">
							<img id="flagImg1" src="https://flagcdn.com/w80/un.png" class="w-16 h-12 object-cover mb-2 rounded shadow-md">
						</div>
                    </label>

                    <label class="cursor-pointer group">
                        <input type="radio" name="prediction" value="N" class="hidden peer">
                        <div class="peer-checked:bg-gray-600 peer-checked:border-transparent bg-gray-800 border-2 border-gray-700 p-6 rounded-2xl text-center transition group-hover:bg-gray-700 h-full">
                            <span class="block text-6xl mb-4">🤝</span>
                            <span class="text-base font-bold text-gray-300 peer-checked:text-white">Nul (N)</span>
                        </div>
                    </label>

                    <label class="cursor-pointer group">
                        <input type="radio" name="prediction" value="2" class="hidden peer">
                        <div class="peer-checked:bg-blue-600 ... flex flex-col items-center">
							<img id="flagImg2" src="https://flagcdn.com/w80/un.png" class="w-16 h-12 object-cover mb-2 rounded shadow-md">
						</div>
                    </label>
                </div>

                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-black py-4 rounded-xl shadow-lg transition text-lg uppercase">Valider mon pari</button>
            </form>
        </section>

        <section>
    <h3 class="text-xl font-bold mb-6 px-2 text-blue-400">Mes Pronostics</h3>
    <div class="grid gap-4">
        <% 
        // Filtrer les paris personnels
        const myBets = bets.filter(b => b.user === user.username);
        myBets.forEach(bet => { %>
            <div class="bg-gray-900 border border-gray-800 p-5 rounded-3xl flex items-center justify-between shadow-lg hover:border-blue-900/30 transition-all">
                
                <div class="flex items-center gap-6">
                    
                    <div class="relative bg-gray-800 p-3 rounded-2xl w-24 flex flex-col items-center">
                        <% if(bet.prediction === '1') { %>
                            <img src="https://flagcdn.com/w80/<%= bet.code1 %>.png" class="w-16 rounded shadow-md mb-2">
                        <% } else if(bet.prediction === '2') { %>
                            <img src="https://flagcdn.com/w80/<%= bet.code2 %>.png" class="w-16 rounded shadow-md mb-2">
                        <% } else { %>
                            <span class="text-3xl mb-2">🤝</span>
                        <% } %>
                        <span class="text-[10px] font-bold text-gray-500 uppercase tracking-widest text-center">Ton choix</span>
                    </div>
                    
                    <div class="flex-1">
                        <p class="text-2xl font-black text-white mb-1"><%= bet.teams %></p>
                        
                        <p class="text-xs text-blue-400 font-semibold flex items-center gap-2">
    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 20 20" fill="currentColor">
        <path d="M10 2a6 6 0 00-6 6v3.586l-.707.707A1 1 0 004 14h12a1 1 0 00.707-1.707L16 11.586V8a6 6 0 00-6-6zM10 18a3 3 0 01-3-3h6a3 3 0 01-3 3z" />
    </svg>
    Vous avez parié : 
    <span class="font-black uppercase text-white ml-1">
        <% 
            // On découpe le nom du match "France - Italie" pour avoir les deux noms séparés
            const teamNames = bet.teams.split(' - ');
            if (bet.prediction === '1') { %>
                <%= teamNames[0] %>
            <% } else if (bet.prediction === '2') { %>
                <%= teamNames[1] %>
            <% } else { %>
                Match Nul
            <% } 
        %>
    </span>
</p>
                    </div>
                </div>

                <button onclick="editBet('<%= bet._id %>', '<%= bet.matchId %>', '<%= bet.prediction %>')" 
                        class="bg-blue-600/10 hover:bg-blue-600 text-blue-400 hover:text-white text-xs font-bold px-5 py-3 rounded-2xl border border-blue-500/20 transition-all flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15.232 5.232l3.536 3.536m-2.036-5.036a2.5 2.5 0 113.536 3.536L6.5 21.036H3v-3.572L16.732 3.732z" />
                    </svg>
                    <span>Modifier</span>
                </button>
            </div>
        <% }) %>
    </div>
</section>
    </main>

    <script>
    function editBet(betId, matchId, prediction) {
        // 1. On remplit le menu déroulant
        const select = document.getElementById('matchSelect');
        
        // On force l'affichage du match même s'il était caché (car déjà parié)
        // On crée une option temporaire si elle n'existe plus dans la liste
        if (!select.querySelector(`option[value="${matchId}"]`)) {
            const tempOpt = document.createElement('option');
            tempOpt.value = matchId;
            tempOpt.text = "Modifier ce match...";
            select.add(tempOpt);
        }
        
        select.value = matchId;
        
        // 2. On met à jour les drapeaux visuels en haut
        select.dispatchEvent(new Event('change'));

        // 3. On coche le bon bouton radio (1, N ou 2)
        const radio = document.querySelector(`input[name="prediction"][value="${prediction}"]`);
        if (radio) radio.checked = true;

        // 4. On prépare le formulaire pour une modification (pas une création)
        let hiddenId = document.getElementById('editingBetId');
        if (!hiddenId) {
            hiddenId = document.createElement('input');
            hiddenId.type = 'hidden';
            hiddenId.name = 'betId';
            hiddenId.id = 'editingBetId';
            document.querySelector('form[action="/bet"]').appendChild(hiddenId);
        }
        hiddenId.value = betId;

        // 5. On change le texte du bouton pour confirmer
        const submitBtn = document.querySelector('form[action="/bet"] button[type="submit"]');
        submitBtn.innerText = "Mettre à jour mon pronostic";
        submitBtn.classList.replace('bg-blue-600', 'bg-yellow-600');

        // 6. Remonter en haut de page doucement
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }
</script>
</body>
</html>